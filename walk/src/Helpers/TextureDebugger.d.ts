import { Texture } from "three";
export default class TextureDebugger {
    private static _scene;
    private static _scale;
    private static _index;
    private static get _numItemInRow();
    static addTexture(texture: Texture): void;
    static render(): void;
}
