import { Vector3, WebGLRenderer } from "three";
export declare const randomSpherePoint: (radius?: number) => Vector3;
export declare const createCubeTextureFromData: (renderer: WebGLRenderer, dataSet: ArrayBufferView[], width: number, height: number) => any;
